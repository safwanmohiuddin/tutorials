<odoo>
    <data>

        <!-- Form View -->
        <record id="course_form_view" model="ir.ui.view">
            <field name="name">student.course.form</field>
            <field name="model">student.course</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <group>
                            <field name="course_name"/>
                            <field name="course_ID"/>
                            <field name="credit_hours"/>
                            <field name="course_active"/>
                            <field name="student"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- List View -->
        <record id="course_list_view" model="ir.ui.view">
            <field name="name">student.course.list</field>
            <field name="model">student.course</field>
            <field name="arch" type="xml">
                <list>
                    <field name="course_name"/>
                    <field name="course_ID"/>
                    <field name="credit_hours"/>
                </list>
            </field>
        </record>

        <!-- Action -->
        <record id="course_action" model="ir.actions.act_window">
            <field name="name">Courses</field>
            <field name="res_model">student.course</field>
            <field name="view_mode">list,form</field>
        </record>

        <!-- Menu -->
        <menuitem id="student_menu_root" name="Student Management"/>
        <menuitem id="course_menu" name="Courses" parent="student_menu_root" action="course_action"/>

    </data>
</odoo>
